<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
      <div class="container">
      <% if signed_in? || facebook_signed_in? %>
        <ul class="nav pull-right">
          <li><%= button_to "Sign out", signout_path, :method=> "delete", :class=>'btn btn-default navbar-btn' %></li>
        </ul>
        <ul class="nav pull-right" style="padding-right:5px">
          <li><%= button_to "Profile", profile_path, :class=>'btn btn-default navbar-btn', :method=>'get' %></li>
        </ul>
        <ul class="nav pull-right" style="padding-right:5px">
        <%=button_to "Upload", {:controller=>"episodes", :action=>"new"}, :class=>'btn btn-default navbar-btn', :method=>'get'%>
        </ul>
      <% else %>
        <ul class="nav pull-right">
          <li><%= button_to "Sign in", signin_path, :class=>'btn btn-default navbar-btn', :method=>'get' %></li>
        </ul>
        <ul class="nav pull-right" style="padding-right:5px">
          <li><%= button_to "Sign up", signup_path, :class=>'btn btn-default navbar-btn', :method=>'get' %></li>
        </ul>
        <ul class="nav pull-right" style="padding-right:5px">
          <%=button_to "Upload", {:controller=>"episodes", :action=>"new"}, :class=>'btn btn-default navbar-btn', :method=>'get'%>
        </ul>
      <% end %>
    </div>
  </div>
</header>